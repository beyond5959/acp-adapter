# @beyond5959/acp-adapter

Run the Codex ACP Go adapter via `npx`:

```bash
npx -y @beyond5959/acp-adapter
```

This package selects the correct prebuilt binary for your OS/CPU pair and launches it with stdio passthrough.
